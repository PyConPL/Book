all:
	scons
	echo Running twice is required to make good table of contents.
test:
	./test_pl.py
compare:
	./gen_cmp.sh
diffs:
	./gen_diff.sh
clean:
	rm -rf build .tmp pyconbook-yyyy.log pyconbook-yyyy.pdf *.pyc .sconsign.dblite
